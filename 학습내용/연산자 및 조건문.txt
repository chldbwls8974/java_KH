연산자 및 조건문
학습일자: 0531~

	[대입, 산술, 증감 연산자]


	[비교(관계) 연산자]
		문자열은 ==로 비교하면 안됨. (문법적으로는 에러가 발생하지 않지만 두 문자열이 같은지 다른지를 제대로 비교하진 않음)
		가능한 경우 :: 저장한 문자열이 리터럴문자열인 경우  //EX) str1과 str2에 리터럴 문자열 abc가 저장된 경우는 ==로 판별이 됨.
		반면에, 입력받은 문자열인 경우 판별 불가 >> 비교하려면 str1.equals(str2); equals를 이용하여 비교해야함.

			※ 문자열을 비교할때에는 되도록이면 equals를 사용하도록 한다.
			※ null과 비교하는 경우 리터럴인지 아닌지와 관계없이 == !=를 이용하여 비교가능하다.


	[논리연산자]
		- 단락회로평가 : 왼쪽 값에 따라 오른쪽이 실행되지 않고 바로 결과가 나오는것


	[복합대입연산자]


	[조건연산자]
		(조건) ? 참 값 : 거짓 값  ,, 값이 String이면 "" 붙이는거 잊지말기


	[비트연산자]



	[ if ] 
		if(조건문){}
		else if(조건문){}
		else{}


	
	[switch]
		변수 == 값 이면 그 케이스의 실행문을 실행하고 break를 통해서 종료한다.
		모두 거짓이면 default를 실행하고 종료한다.
		

		switch (변수 또는 식)
		case 값1:                                     ※ 값에 들어갈 수 있는 자료형은 정수, 문자, 문자열만 가능하다.
			실행문;			   또한, 값은 리터럴 또는 상수만 가능하다.
			break;			
		.				
		.
		.
		
		default:
			실행문;


		- break : 조건문을 멈춰주는 역할
			break를 만날때 까지 실행문을 차례로 모두 실행시킨다.

		- continue

